<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- L1-UPGRADE (OTP maximum assistance): SMS User Consent API —
         koi SMS permission NAHI chahiye. OTP dialog khulne par Play
         Services SMS sunta hai; user ek tap me message padhne ki
         permission deta hai, OTP field me auto-bharta hai. -->
    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="34" />
    <application
        android:name=".FmApp"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- v22: crash report screen (CrashCatcher ise kholta hai) -->
        <activity
            android:name=".CrashReportActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
        <!-- operator console: fullscreen immersive remote-control view -->
        <activity
            android:name=".agent.OperatorView"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <service
            android:name=".engine.FormRunService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- v24 N1: Working Mode ON par persistent notification (foreground
             service). FOREGROUND_SERVICE permission upar declared hai. -->
        <service
            android:name=".agent.WorkingModeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- J1: FCM push receive (Firebase project clip-flow-685a5 reuse).
             FCM configured na ho to ye service kabhi trigger nahi hoti —
             polling fallback tab bhi kaam karta hai. -->
        <service
            android:name=".agent.FmMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
